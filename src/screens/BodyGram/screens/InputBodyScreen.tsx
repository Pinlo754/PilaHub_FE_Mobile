// InputBodyScreen removed — screen deleted per request.
// If you need to restore it later, retrieve previous version from git history.
import React from 'react';
import { View } from 'react-native';

export default function InputBodyScreen() {
  // intentionally render nothing; screen removed
  return <View />;
}
