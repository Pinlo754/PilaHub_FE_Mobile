import React from 'react';
import { View } from 'react-native';

// Placeholder: Image carousel removed — kept file to avoid import errors elsewhere.
export default function ImageCarousel() {
  return <View />;
}
